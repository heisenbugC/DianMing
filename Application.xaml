<Application x:Class="Application"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    StartupUri="MainWindow.xaml"
    ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Aero theme dictionary from PresentationFramework.Aero -->
                <ResourceDictionary Source="pack://application:,,,/PresentationFramework.Aero;component/themes/Aero.NormalColor.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Global Window styling -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            </Style>

            <!-- TextBox styling -->
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="2" />
            </Style>

            <!-- ListBoxItem styling for a bit more vertical space -->
            <Style TargetType="ListBoxItem">
                <Setter Property="Padding" Value="6,3" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
